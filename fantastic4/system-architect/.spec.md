# .spec.md — system-architect: Architecture Spec with ADR-First Decision Tracking

## 1. Objective

Minor refinements — system-architect is already well-structured. Focus on removing redundant metadata, pipeline-relative references, and adding ADR-first decision tracking.

## 2. Key Changes

| Current | Future |
|---------|--------|
| `stage: "skill-3-of-4"` in metadata | Removed (redundant with pipeline frontmatter) |
| "Skill 4" in body refs | Pipeline-relative references (where inaccurate) |

## 3. Files to Modify

| File | Changes |
|------|---------|
| `system-architect/SKILL.md` | Remove `stage` from metadata; verify all "Skill N" refs are accurate |

## 4. Acceptance Criteria

1. No redundant `stage` field in frontmatter
2. Pipeline references use names (not numbers)
