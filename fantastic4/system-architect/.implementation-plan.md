# Implementation Plan — system-architect: Cleanup

> **2 steps.**

## Step 1: Remove `stage` from frontmatter
Remove `stage: "skill-3-of-4"` (redundant with pipeline frontmatter).

## Step 2: Remove "Skill 3" from body
Line 29: Change `"Skill 4 (context-engineer)"` to just `"context-engineer"`.

## Verify
- ✅ grep "Skill \d" returns 0 matches in SKILL.md
- ✅ pipeline frontmatter intact
