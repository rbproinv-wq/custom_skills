# .spec.md — domain-researcher: Optional Domain Research with Legal Validation

## 1. Objective

Minimal cleanup — domain-researcher is clean. Remove redundant stage metadata.

## 2. Key Changes

| Current | Future |
|---------|--------|
| `stage: "optional-phase"` in metadata | Removed (pipeline frontmatter `optional: true` defines this) |

## 3. Files to Modify

| File | Changes |
|------|---------|
| `domain-researcher/SKILL.md` | Remove `stage` from frontmatter metadata |

## 4. Acceptance Criteria

1. No redundant `stage` field in frontmatter
2. All functionality preserved
