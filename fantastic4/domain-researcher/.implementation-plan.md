# Implementation Plan — domain-researcher: Cleanup

> **1 step.**

## Step 1: Remove `stage` from frontmatter
Remove `stage: "optional-phase"`.

## Verify
- ✅ No `stage` field in metadata
- ✅ Pipeline frontmatter `optional: true` intact
