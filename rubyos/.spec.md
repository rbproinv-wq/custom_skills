# .spec.md — rubyos: Path Flexibility + Security Notes

## 1. Objective

Minimal hardening — add fallback path logic for non-standard installations, document security boundaries.

## 2. Key Changes

| Current | Future |
|--------|--------|
| `~/.RubyOs/` hardcoded | Add fallback check: if `~/.RubyOs/` not found, check `$RUBYOS_HOME` env var |
| Security concern (sensitive data in skill dir) | Documentation on secure data handling |

## 3. Files to Modify

| File | Changes |
|------|---------|
| `SKILL.md` | Add path fallback note + security boundary section |

## 4. Acceptance Criteria

1. SKILL.md includes note about `$RUBYOS_HOME` fallback
2. Security boundary documented (what should NOT be in skill directory)
