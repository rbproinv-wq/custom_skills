# Implementation Plan — rubyos: Path Flexibility + Security

> **2 steps.**

## Step 1: Add `$RUBYOS_HOME` fallback note
In the Installation section, update:
```markdown
RubyOs is installed at `~/.RubyOs/` (or `$RUBYOS_HOME` if set). Verify:
```

## Step 2: Add Security Boundary section
```markdown
## 🔒 Security Boundary
- **Never** store API keys, credentials, or secrets in the skill directory
- The `~/.RubyOs/` directory may contain MCP credentials — ensure `.gitignore` excludes it
- Sensitive data should use environment variables or a secrets manager
```

## Verify
- ✅ `$RUBYOS_HOME` fallback documented
- ✅ Security boundary section present
